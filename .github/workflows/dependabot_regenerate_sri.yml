name: "Regenerate SRI on dependabot PRs"
on:
  pull_request:
    paths:
      - go.mod
      - go.sum

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  update_dependabot_pr:
    if: github.event.pull_request.user.login == 'dependabot[bot]' && github.repository == 'boinkor-net/disk-spinner'
    runs-on: ubuntu-latest
    steps:
      - name: Generate token
        id: generate_token
        uses: tibdex/github-app-token@v2
        with:
          app_id: ${{ vars.PR_FIXUP_APP_ID }}
          private_key: ${{ secrets.PR_FIXUP_APP_PRIVATE_KEY }}
          permissions: >-
            {"contents": "write"}
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{steps.generate_token.outputs.token}}
      - uses: DeterminateSystems/nix-installer-action@main
      - uses: cachix/cachix-action@v17
        with:
          name: disk-spinner
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          skipPush: true
      - name: re-generate SRI
        run: "nix --extra-experimental-features nix-command --extra-experimental-features flakes run .#generate-sri-default"
        working-directory: "."

      - name: Push changes to ${{github.base_ref}}
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: "Update gomod SRI hash"
          file_pattern: "*.sri"
